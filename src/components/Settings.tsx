import { ContentWrapper, ContentHead, HeadTitle } from "./Users";

const Settings: React.FC = () => {
  return (
    <ContentWrapper>
      <ContentHead>
        <HeadTitle>Settings</HeadTitle>
      </ContentHead>
    </ContentWrapper>
  );
};

export default Settings;
